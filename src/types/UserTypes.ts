export type UserTypes = {
  id: string;
  name: string;
  username: string;
  active: boolean;
  email: string;
  role: string;
  phone: string;
  first_access: boolean;
  description?: string;
  created_at: Date;
};
